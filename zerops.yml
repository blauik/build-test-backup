dotnet0:
  build:
    base:
      - dotnet@6
    build:
      - dotnet --info
      - dotnet build -o app
    deploy:
      - app
  run:
    init:
      - dotnet --info
    start: cd app && dotnet dnet.dll
